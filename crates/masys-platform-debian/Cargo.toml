[package]
name = "masys-platform-debian"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Debian platform facts for masys: pending reboot, unit ownership, kernel images in /boot."
readme = "../../README.md"
keywords = ["masys", "debian", "ubuntu", "systemd", "linux"]
categories = ["command-line-utilities", "os::linux-apis"]

[dependencies]
masys-domain = { path = "../masys-domain", version = "0.1.0" }
